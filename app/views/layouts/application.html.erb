<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Forest Critter Crafting</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'products/header_nav' %>

    <% if !notice.blank? || !alert.blank? %>
      <div class="notification is-link my-0">
        <p class="notice has-text-centered mb-0"><%= notice %></p>
        <p class="alert has-text-centered mb-0"><%= alert %></p>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
